# Development environment configuration for Windows Docker Desktop
# Database configuration (Docker service names)
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=safecompanions
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres123

# Flask configuration
FLASK_APP=app.py
FLASK_ENV=development

# Nginx configuration (use dev config for local development)
NGINX_CONF_FILE=nginx.dev.conf

